<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Add_LichHen"></activity>

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/vien" />

        <activity android:name=".Lich_hen" />
        <activity android:name=".Add_group" />
        <activity
            android:name=".Add_member"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- <meta-data -->


            <!-- android:name="android.support.PARENT_ACTIVITY" -->


            <!-- android:value="com.example.qlsdc.MainActivity" /> -->

        </activity> <!-- android:parentActivityName=".MainActivity" -->
        <activity
            android:name=".Info_member"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".MainActivity"></activity>
        <!--<receiver android:name=".Notificatuon.Notifica_Receiver"/>-->

        <!--<service android:name=".Notificatuon.MyNotificationManager"-->
            <!--android:enabled="true"/>-->
        <!--<service android:name=".Notificatuon.myService"-->
            <!--android:enabled="true"-->

            <!--/>-->
    </application>

</manifest>